# Busha Pay React Native SDK

🔜 Placeholder — React Native SDK coming soon.

When implemented, the SDK will live under `react-native/`, with the shared WebView checkout page synced from [`/shared/busha_pay_checkout.html`](../shared/busha_pay_checkout.html) into `react-native/assets/busha_pay_checkout.html` via `make sync`.
