org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official

# Published artifact version. Bumped by the release workflow and the
# JitPack mirror; consumed by pay-android/build.gradle.kts.
VERSION=0.0.1
