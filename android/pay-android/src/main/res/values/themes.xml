<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Translucent theme for the WebView checkout Activity so the
         merchant's screen stays dimmed behind it, like a modal sheet.
         Parented to a framework theme — no AppCompat dependency. -->
    <style name="BushaPay.CheckoutTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Activity</item>
    </style>
</resources>
